A contact enquiry was made by {{ contact.name }} at {{ "now" | date("Y-m-d H:i") }}.<br/>

Reply-To: {{ contact.email }}<br/>
Subject: {{ contact.subject }}<br/>
Description:<br/>
{{ contact.description }}
